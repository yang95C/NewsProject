package com.yg.common.router

class RouterActivityPath {

    object Main {
        private const val MAIN = "/main"

        // 主页面
        const val PAGER_MAIN = "$MAIN/Main"
    }

    object User {
        private const val USER = "/user"

        const val PAGER_MY = "$USER/My"
    }
}